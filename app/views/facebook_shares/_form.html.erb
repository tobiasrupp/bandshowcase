<%= form_for(@facebook_share) do |f| %>
  <% if @facebook_share.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@facebook_share.errors.count, "error") %> prohibited this facebook_share from being saved:</h2>

      <ul>
      <% @facebook_share.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :post_id %><br>
    <%= f.text_field :post_id %>
  </div>
  <div class="field">
    <%= f.label :sharecount %><br>
    <%= f.number_field :sharecount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
